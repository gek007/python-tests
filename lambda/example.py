# write simple lambda function that returns the square of a number

square = lambda x: x**2
print(square(5))

# write a lambda function that returns the sum of two numbers

sum = lambda x, y: x + y
print(sum(5, 10))

# write a lambda function that returns the length of a string

length = lambda x: len(x)
print(length("Hello"))


sum2 = lambda x, y: x + y
print(sum2(5, 10))
